import {getConfigSetList, insertUserConfig} from '../../../services/myService';

/**
 * 登录页 Model
 */
const myModel = {
  namespace: 'my',

  state: {
  },

  effects: {},

  reducers: {},
};

export default myModel;
